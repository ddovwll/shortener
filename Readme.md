````markdown
# URL Shortener

- Создание коротких ссылок для длинных URL.
- Получение оригинального URL по короткому коду.
- Учёт визитов (IP, User-Agent, время).

---

## 🛠 Используемые технологии

### Backend:
- **Go** — основной язык.
- **Chi** — лёгкий HTTP-роутер.
- **PostgreSQL** — основная база данных.
- **Kafka — асинхронная обработка событий.
- **Slog logger** — структурированное логирование.
- **Swaggo** — генерация Swagger-документации.

### Инфраструктура:
- **Docker / Docker Compose** — упрощённый запуск.
- **Makefile** — команды для разработки и сборки.

---

## 📦 Запуск проекта

### 🔧 1. Запуск через Docker Compose

```bash
docker-compose up --build
````

После запуска сервис будет доступен по адресу:

* API: **[http://localhost:8080](http://localhost:8080)**
* Swagger: **[http://localhost:8080/swagger/index.html](http://localhost:8080/swagger/index.html)**

---

## 🧪 Запуск тестов

```bash
go test ./... -v
```

Для покрытия:

```bash
go test ./... -cover
```

---

## 🌍 Переменные окружения

| Переменная               | Описание                         | Значение по умолчанию                                           |
| ------------------------ | -------------------------------- | --------------------------------------------------------------- |
| **LOG_LEVEL**            | Уровень логирования              | `info`                                                          |
| **PG_DSN**               | Строка подключения к PostgreSQL  | `postgres://postgres:postgres@db:5432/postgres?sslmode=disable` |
| **PG_MAX_OPEN_CONNS**    | Максимум открытых соединений     | `50`                                                            |
| **PG_MAX_IDLE_CONNS**    | Максимум неактивных соединений   | `10`                                                            |
| **PG_CONN_MAX_LIFETIME** | Максимальный lifetime соединения | `1m`                                                            |
| **KAFKA_BROKER**         | Адрес Kafka брокера              | `kafka:9092`                                                    |
| **KAFKA_TOPIC**          | Топик для отправки визитов       | `visits`                                                        |
| **KAFKA_GROUP_ID**       | Группа consumer'ов               | `visits-group`                                                  |
| **HTTP_PORT**            | Порт HTTP-сервера                | `:8080`                                                         |
| **REDIS_HOST**           | Адрес Redis                      | `redis:6379`                                                    |
| **REDIS_PASSWORD**       | Пароль Redis                     | `` (пусто)                                                      |
| **REDIS_DB**             | Номер базы Redis                 | `0`                                                             |

---

## 🔗 Эндпоинты API

### 📌 Создание короткой ссылки

**POST /shorten**

Тело:

```json
{
  "shortCode": "custom", // необязательно
  "originalURL": "https://example.com"
}
```

Ответ:

```json
{
  "shortCode": "abc123",
  "shortURL": "http://localhost:8080/s/abc123"
}
```

---

### 📌 Переход по короткой ссылке

**GET /s/{short_code}**

Редирект на оригинальный URL

---

### 📌 Получение статистики

**GET /analytics/{short_code}?group=day|month|userAgent**

Ответ:

```json
[
    {
      "period": "2025-12-01",
      "count": 15
    }
]
```

---

### 📌 Swagger документация

**GET /swagger/**

---

## 📁 Структура проекта

```
shortener/
├─ src/
│  ├─ cmd/
│  │  └─ main.go                    # Точка входа
│  ├─ internal/
│  │  ├─ application/
│  │  │  ├─ config/                 # Конфигурация приложения
│  │  │  ├─ contracts/              # Интерфейсы
│  │  │  └─ services/               # Сервисы и бизнес-логика
│  │  ├─ domain/
│  │  │  ├─ short_link/             # Доменные модели ссылок
│  │  │  └─ visit/                  # Доменные модели визитов
│  │  ├─ infrastructure/
│  │  │  ├─ cache/                  # Кэширование
│  │  │  ├─ data/                   # Репозитории
│  │  │  ├─ kafka/                  # Kafka producer/consumer
│  │  │  └─ short_link_generator/   # Генератор коротких кодов
│  │  └─ web_api/
│  │     ├─ controllers/            # HTTP контроллеры
│  │     ├─ docs/                   # Swagger доки
│  │     ├─ models/                 # DTO модели
│  │     └─ public/                 # Статические файлы
├─ pkg/
│  └─ logger/                       # Логирование
├─ .gitignore
├─ .golangci.yml                    # Настройки линтера
├─ docker-compose.yml
├─ Dockerfile
├─ Makefile
├─ go.mod
└─ task.md
```